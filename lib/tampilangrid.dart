import 'package:flutter/material.dart';

class TampilanGrid extends StatelessWidget {
  final int gridCount;

  const TampilanGrid({Key? key, required this.gridCount}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //body: ,
    );
  }
}